# Flutter plugins ship their own consumer ProGuard rules bundled in their
# AARs (picked up automatically by AGP), so this file only needs
# project-specific exceptions if/when R8 strips something it shouldn't.
