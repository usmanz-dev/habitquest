/// Shop cosmetic ids (HabitQuest_PRD.md §7 screen 11), shared between
/// ShopScreen (where they're purchased) and the widgets that render each
/// effect — [AvatarDisplay], [HabitCard], AvatarProfileScreen.
class CosmeticIds {
  CosmeticIds._();

  static const neonTrail = 'neon_trail';
  static const goldenFrame = 'golden_frame';
  static const confettiBurst = 'confetti_burst';
  static const starryBackdrop = 'starry_backdrop';
  static const rainbowGlow = 'rainbow_glow';
}
